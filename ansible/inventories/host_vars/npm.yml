---
stacks:
  - name: caddy
    src: network-services/caddy

# Reached through the proxy with real SNI, so these prove the route table works.
proxy_checks:
  - { host: music.neophantom.com }
  - { host: password.neophantom.com }
  - { host: torrent.neophantom.com }
  - { host: immich.neophantom.com }
  - { host: adguard.neophantom.com }
