#FILEBROWSER MOUNT POINTS
CLOUD_MOUNT=/path/to/cloud
LLM_MOUNT=/path/to/llm

#FILEBROWSER AUTH
FILEBROWSER_ADMIN_PASSWORD=CHANGE-ME

#ONLYOFFICE JWT (must match between filebrowser and onlyoffice)
# Generate: openssl rand -base64 32
ONLYOFFICE_JWT_SECRET=CHANGE-ME

#SYNCTHING MOUNT POINTS
SYNC_MOUNT=/path/to/sync
