# Copy to .env in this directory (docker compose auto-loads it).
# .env is gitignored and must never be committed.
SLSKD_API_KEY=YOUR_KEY_HERE
SLSKD_USERNAME=YOUR_USERNAME
SLSKD_PASSWORD=YOUR_PASSWORD
